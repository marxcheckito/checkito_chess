import XCTest
@testable import checkito_chess

final class checkito_chessTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(checkito_chess().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
